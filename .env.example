
# =============================
#   SPOTIFY2MQTT ENV EXAMPLE  #
# =============================

# --- SPOTIFY API (Get these at https://developer.spotify.com/dashboard/applications) ---
SPOTIFY_CLIENT_ID=your_id           # Required: Spotify Client ID
SPOTIFY_CLIENT_SECRET=your_secret   # Required: Spotify Client Secret
SPOTIFY_REFRESH_TOKEN=              # Leave empty until authorized. Paste your refresh token here after auth.

# --- MQTT BROKER ---
MQTT_URL=mqtt://mosquitto:1883      # Required: MQTT broker URL (e.g. mqtt://host:port)
MQTT_USER=your_username             # Optional: MQTT username
MQTT_PASS=your_password             # Optional: MQTT password
MQTT_TOPIC_BASE=spotify2mqtt/spotify # Topic base for published data

# --- GENERAL SETTINGS ---
POLL_MS=2000                        # Polling interval in milliseconds (default: 2000)
AUTH_PORT=8889                      # Port for local auth server (must match Spotify Redirect URI)
REDIRECT_HOST=127.0.0.1             # Host for redirect (use 127.0.0.1, not localhost)

# --- NOTES ---
# - Set AUTH_PORT and REDIRECT_HOST to match your Spotify App Redirect URI (e.g. http://127.0.0.1:8889/callback)
# - After authorization, copy the refresh token into SPOTIFY_REFRESH_TOKEN
# - MQTT_USER and MQTT_PASS are optional for unsecured brokers